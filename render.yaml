services:
  - type: web
    name: seq-transit-predictor-lite
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: >
      uvicorn app.main_lite:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATA_DOWNLOAD_URL
        sync: false
